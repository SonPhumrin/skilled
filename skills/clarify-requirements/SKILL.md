---
name: clarify-requirements
description: A relentless interview to sharpen a plan or design before any code is written.
disable-model-invocation: true
---

Call the Skill tool with "requirements-interview".

Save nothing to disk. This is the stateless entry point, for sharpening a plan, a design, or a piece of writing with no repository under it.

When there is a repository, tell the user `/domain-interview` runs the same interview and leaves a paper trail in `CONTEXT.md` and ADRs, which makes it the better choice.
