# Context: <project name>

The domain language for this project. Every agent working here uses these terms exactly, and avoids the listed alternatives. Shared vocabulary is why an agent can say "the Reconciliation runs nightly" instead of re-describing the concept every turn.

Keep this file short. A term earns a place here when it is used across the codebase and its meaning is not obvious from the name.

## Language

**<Term>**:
<One or two sentences. What it is, and the distinction that matters.>
_Avoid_: <synonyms that must not be used for this concept, and why they mislead>

**<Term>**:
<Definition.>
_Avoid_: <synonyms>

## Relationships

- A **<Term>** has many **<Term>**
- A **<Term>** carries one **<Term>** at a time
- A **<Term>** is a kind of **<Term>**

## Flagged ambiguities

Terms that were used two ways, and how it was resolved. Keeping the history here stops the ambiguity creeping back.

- "<word>" meant both <X> and <Y>. Resolved: <X> is **<Term>**; <Y> is **<Term>**. "<word>" is no longer used.
